# Advisor Appendix — Cold Onboarder (DP-A verification)

## Advisor
Cold Onboarder Advisor

## Verdict
CLOSED — the default is now unambiguous: Council → Chair is the core product; Executor/Reviewer only run on explicit request.

## Evidence
- **README.md lines 5–14:** "At its core, the council runs two phases: `Council → Chair`. **This is the default.** Asking to 'council this' runs Council → Chair and returns a recommendation. That is the product."
- **SKILL.md lines 17–20:** "Two further phases are **optional add-ons that make the Chair's recommendation actionable**, run only on explicit request" — explicitly labels Executor and Reviewer as opt-in.
- **SKILL.md "Default flow and optional extensions":** separates "Default flow — always" (Council + Chair, stop here) from "Optional extensions — only on explicit request" (Executor, Reviewer).
- **README.md lines 16–25:** explicit opt-in triggers — "asking for 'a full council,' 'council with execution,' or a follow-up…"

## Residual or newly-introduced issues
None — the docs consistently present Council → Chair as the core/default and Executor/Reviewer as optional extensions triggered by explicit signals.

## Confidence
High — as a first-time reader I came away with immediate clarity: "council this" means perspectives + recommendation, full stop; to get artifacts or verification, you must ask. The docs lead with that, repeat it, and separate default from opt-in consistently.
